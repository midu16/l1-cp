---
# LVMS shares namespace openshift-storage with ODF; Namespace and OperatorGroup
# are created by hub-config/operators-deployment/99_01_odf_deployment.yaml.
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  name: lvms-operator
  namespace: openshift-storage
spec:
  channel: stable-4.21
  installPlanApproval: Automatic
  name: lvms-operator
  source: redhat-operators-disconnected
  sourceNamespace: openshift-marketplace

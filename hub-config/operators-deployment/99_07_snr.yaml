---
# Namespace + OperatorGroup match 99_06_nhc.yaml (one OG per namespace; idempotent if both are applied).
apiVersion: v1
kind: Namespace
metadata:
  name: openshift-workload-availability
---
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: node-healthcheck-operator
  namespace: openshift-workload-availability
  annotations:
    operatorframework.io/bundle-unpack-min-retry-interval: 10m
spec: {}
---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: self-node-remediation
  namespace: openshift-workload-availability
spec:
  channel: "stable"
  name: self-node-remediation
  source: cs-redhat-operators-disconnected-2-v4-18
  sourceNamespace: openshift-marketplace
  installPlanApproval: Manual
